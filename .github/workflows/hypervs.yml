name: HyperVS_Auto_Build

on:
  workflow_dispatch:
    inputs:
      ROM_URL:
        description: 'OTA Url'
        required: false
        default: ''
      vip:
        type: boolean
        description: Vietsub Pro
        default: true
      stock:
        type: boolean
        description: Build Stock fastboot Rom
        default: false
      custom:
        type: boolean
        description: Custom branch
        default: false
      key:
        description: 'Active Key'
        required: true
        default: ''

jobs:
  BuildAuthorizedKey:
    if: ${{ github.event.inputs.key == vars.AUTH_KEY }}
    runs-on: ubuntu-latest
    environment: AutoBuild
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      # Add more steps as needed for the build process

      # Add more steps or actions as needed for handling unauthorized access
